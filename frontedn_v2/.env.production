# Production: leave REACT_APP_SITE_BACKEND empty so the frontend uses relative
# URLs (/api/v1/...). This way API calls go to the same origin that served the
# page, which works regardless of hostname or IP.
REACT_APP_SITE_BACKEND=
